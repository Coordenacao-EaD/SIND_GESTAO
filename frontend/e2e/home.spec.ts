/**
 * Placeholder for the future Playwright E2E suite covering the Home page
 * (desktop/mobile viewports, full keyboard navigation, real browser
 * rendering of responsive breakpoints).
 *
 * Playwright is NOT installed in this phase — running `npm run test`
 * (Vitest) does not execute this file, and there is no `test:e2e` script
 * yet. This file exists only to reserve the location described in the
 * frontend architecture.
 *
 * To activate it in a future phase:
 *   1. npm install -D @playwright/test && npx playwright install
 *   2. Add a playwright.config.ts pointing testDir at "./e2e"
 *   3. Add a "test:e2e": "playwright test" script to package.json
 *   4. Replace this stub with real scenarios (e.g. visiting "/", asserting
 *      the hero heading, resizing to the required breakpoints, tabbing
 *      through the header).
 */
export {};
